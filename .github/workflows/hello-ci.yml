name: Greeting Workflow
on: [push]

jobs:
 say-hello:
  runs-on: ubuntu-latest
  steps:
   # - name: Run a greeting
   #   run: echo "the CI pipeline has succesfully started!"

   - uses: actions/checkout@v4
   
   - name: Run Python Tests
     run: python3 test_calc.py
     
